[project]
name = "wechat-decrypt-tool"
version = "2.5.1"
description = "Modern WeChat database decryption tool with React frontend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cryptography>=41.0.0",
    "fastapi>=0.104.0",
    "uvicorn[standard]>=0.24.0",
    "python-multipart>=0.0.6",
    "aiofiles>=23.2.1",
    "typing-extensions>=4.8.0",
    "pywin32>=310; sys_platform == 'win32'",
    "psutil>=7.0.0",
    "pycryptodome>=3.23.0",
    "requests>=2.32.4",
    "loguru>=0.7.0",
    "zstandard>=0.23.0",
    "pilk>=0.2.4",
    "pypinyin>=0.53.0",
    "jieba>=0.42.1",
    "wx_key>=2.0.1; sys_platform == 'win32'",
    "pefile>=2024.8.26; sys_platform == 'win32'",
    "pymem>=1.14.0; sys_platform == 'win32'",
    "yara-python>=4.5.2; sys_platform == 'win32'",
    "packaging",
    "httpx",
    "watchfiles>=1.1.0",
    "langchain-core>=1.0,<2",
    "langchain-openai>=1.0,<2",
    "langchain-anthropic>=1.0,<2",
    "langgraph>=1.0,<2",
    "langgraph-checkpoint-sqlite>=3.0,<4",
    "pypdf>=5.0",
    "pypdfium2>=4.30",
    "python-docx>=1.1",
    "openpyxl>=3.1",
    "python-pptx>=1.0",
    "Pillow>=10.0",
    "huggingface-hub==1.27.0",
    "onnxruntime==1.28.0; sys_platform != 'darwin'",
    # macOS 固定使用同时提供 Apple Silicon / Intel、macOS 13+ wheel 的 CPU 版本。
    "onnxruntime==1.23.2; sys_platform == 'darwin'",
    "tokenizers==0.23.1",
    "sqlite-vec==0.1.9",
    "deepagents==0.7.13",
]

[project.optional-dependencies]
build = [
    "pyinstaller>=6.0.0",
]
voice-transcription = [
    "faster-whisper>=1.1.0",
    "opencc-python-reimplemented>=0.1.7",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]

[project.scripts]
wechat-decrypt = "main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/wechat_decrypt_tool"]
include = [
    "src/wechat_decrypt_tool/native/VoipEngine.dll",
    "src/wechat_decrypt_tool/native/wce_integrity*.pyd",
    "src/wechat_decrypt_tool/native/wechatdb_client.dll",
    "src/wechat_decrypt_tool/native/wechatdb_broker.exe",
    "src/wechat_decrypt_tool/native/libwechatdb_client.dylib",
    "src/wechat_decrypt_tool/native/wechatdb_broker",
    "src/wechat_decrypt_tool/native/wechatdb_native_build.json",
    "src/wechat_decrypt_tool/native/weflow_wasm/weflow_wasm_keystream.js",
    "src/wechat_decrypt_tool/native/weflow_wasm/wasm_video_decode.js",
    "src/wechat_decrypt_tool/native/weflow_wasm/wasm_video_decode.wasm",
    "src/wechat_decrypt_tool/native/weflow_wasm/sns_image_fixture.json",
    "src/wechat_decrypt_tool/native/macos/source/*.c",
    "src/wechat_decrypt_tool/resources/*.json",
]

[tool.uv]
find-links = ["./tools/key_wheels/"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# 让 pytest 命令与 python -m pytest 都能导入仓库工具和源码。
pythonpath = [".", "src"]
